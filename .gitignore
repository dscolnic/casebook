# macOS / editor cruft
.DS_Store
**/.DS_Store
**/.claude/*
!**/.claude/skills/
CLAUDE.md.bak

# dependencies
node_modules/

# batch driver runtime output (regenerated)
*.log
batch/launchd.out
batch/launchd.err

# SECRETS — never commit your Anthropic key
.env
*.pem
*.key
apikey*

# Vite build output (Deep Watch is built in CI for Pages)
deep_watch/dist/

# gamekit build output (per-theme)
Alamos/gamekit/dist/

# The two shipped games: source only, never their builds or backups
Alamos/Hospital/hospital-fps/dist/
Alamos/project-y-fps/dist/
*.bak
Alamos/Hospital/hospital-fps/project-y-fps/

# Replit working copy of the casebook app — agent state under .local/ includes
# an environment-secrets skill, so this directory stays out of the repo.
casebook_replit/

# Third-party PDFs kept locally for reference, not ours to redistribute.
Games _ The New York Times Company.pdf
Making an Impact by Avoiding an Impact _ Duke Pratt School of Engineering.pdf
