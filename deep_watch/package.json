{
  "name": "deep-watch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Deep Watch — Reasoning Under Pressure Beneath the Surface. A first-person submarine operations game that integrates the RECKON Navy-course reasoning games as in-world systems.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "test": "playwright test",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
