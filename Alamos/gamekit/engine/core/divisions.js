// divisions.js — the active theme's areas of study. See engine/core/theme.js.
//
// The name is the original vocabulary from the Los Alamos build, kept because
// renaming it touches every core file. A "division" here is a group.
import { CONTENT } from './theme.js';

export const GROUP_DEFS = CONTENT.GROUPS ?? [];
