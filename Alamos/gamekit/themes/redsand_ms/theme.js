// theme.js — Red Sand, at grade 6.
//
// edition-of: redsand
//
// GENERATED by tools/derive-edition.mjs. This is an *edition*: the place, the
// props, the interiors and the outfits are themes/redsand's and are imported
// across, so this directory owns a manifest and its generated content and
// nothing else. engine/dev/editionParity.mjs fails the game for any other file
// in here, and for a cast or a set of areas that has drifted from the base.
//
// The marker line above is read by engine/dev/registry.mjs (placeDir) and by
// vite.config.js, which take the site kind and the world module from the base
// theme — this directory's site.js is a re-export and no regex can read it.
import { site } from '../redsand/site.js';
import { OUTFITS, roleToOutfit } from '../redsand/outfits.js';
import { GROUPS } from './content/groups.js';
import { MISSIONS, WARMUPS } from './content/missions.js';
// tools/import-book.mjs writes all of these. BALLPARK_CALCS and JARGON must be
// imported or the estimates render un-answerable and no term is clickable.
import { CURRICULUM, BALLPARK_CALCS, JARGON } from './content/curriculum.js';
import { ROSTER, LEADERS, AVATARS } from './content/roster.js';
import { COPY } from './content/copy.js';
import { INTERIORS } from '../redsand/interiors.js';
// The junior edition shares the base game's site, so it inherits the seven places
// the placement pass opened. Without the minor interiors their doors would be
// registered and open nothing — see themes/redsand/minors.js.
import { MINOR_INTERIORS } from '../redsand/minors.js';
import { decorate, fitOutRoom, fitOutSpine } from '../redsand/props.js';

export default {
  // Who this edition is for. `engine/core/typography.js` reads it and scales the
  // root font size, so the same game can ship at several reading levels.
  // grade 4 scales 1.18x, 7 scales 1.10x, 13 and up not at all.
  audience: { grade: 6 },

  id: 'redsand_ms',
  title: 'Red Sand',
  subtitle: 'Propellant Lead · Arcadia Rise',

  // A mission is one Martian day. `dayNoun` reaches the plan card, the
  // continuity line, the turn-in button and the campaign log, and a sol is
  // thirty-nine minutes longer than a day — which is why the boil-off and the
  // electrolysis arithmetic in this game are all done over 88,800 seconds.
  dayNoun: 'Sol',
  // What a non-person stop is called here. Nothing on this station is a room:
  // they are modules, buried to the eaves, and a player told to go to a room
  // looks for a building with windows.
  stopNoun: 'module',

  // The place. `site.kind` picks the world module in vite.config.js:
  //   'outdoor'   engine/world/outdoorTown.js — buildings on terrain
  //   'interior'  engine/world/interiorSite.js — a spine with rooms off it
  // A theme whose place already exists may declare its own instead, with
  // `world: 'themes/<name>/world.js'` inside site.js. Deep Watch does.
  site,

  // Where the player starts the day, and which way they face. The day's budget
  // is measured from here, not from wherever the player is standing.
  start: site.spawn,

  content: { GROUPS, MISSIONS, CURRICULUM, BALLPARK_CALCS, JARGON, ROSTER, LEADERS, AVATARS, COPY, WARMUPS },

  people: {
    OUTFITS,
    roleToOutfit,
    // spawn must be >= ROSTER.length, or characters past the limit never appear
    // and any mission stop naming them is unreachable. Validated.
    spawn: ROSTER.length,
    // Background people. A narrow place needs far fewer: on the submarine more
    // than eight and the player cannot get down the passage.
    extras: 18,
  },

  // What is inside each room the player walks into, from book.yml. Rooms are
  // built by engine/world/interiorBuilding.js on first entry, in a district
  // four kilometres from the town.
  interiors: { ...INTERIORS, ...MINOR_INTERIORS },
  // How those rooms are built: 'lab' (vinyl, screens), 'timber' (board walls,
  // chalkboards, no screens anywhere) or 'steel' (painted plate, deck matting).
  interiorStyle: 'steel',

  // The title card: ONE paragraph. What the player is, where they are, and what
  // happens if the work is not done — the situation, and nothing else. The
  // rules of a day (order, clock, the price of a wrong call) used to be a
  // second paragraph here and in every game, and it was the part nobody read:
  // four sentences of mechanics standing between the player and the game, all
  // of it discoverable in the first minute of play or from the plan card.
  // ---------------------------------------------------------- the delivery
  //
  // What the fortnight produces, and the one room the parts of it are kept in.
  // The opening card names it, the plan card says which piece today is, the card
  // that closes a day hands that piece over, and the board in the room named by
  // `where` is where all of them can be seen at once — engine/core/delivery.js.
  //
  // Ten sols, so ten pieces. A handover says what is known, how well, and what
  // was never established at all.
  delivery: {
    name: 'The Propellant Handover',
    what: 'What the next crew is handed. Every batch signed against the test it came with, the '
      + 'plant\'s real limits, and what nobody here ever settled.',
    where: 'GIBBS',
    pieces: [
      'The rate and the ceiling',
      'The hotter-reactor argument',
      'The blocked line finding',
      'The recycle loop',
      'The water plant first',
      'The assay result',
      'The new charge rule',
      'The low-power decision',
      'The loading decision',
      'The handover, signed',
    ],
  },
  opening: [
    'The window opens on sol 486 and not again for twenty-six months. Six people are at '
    + 'Arcadia Rise, and none of them goes home unless the vehicle on the pad is full. The '
    + 'fuel is made here, out of the air and the ground of Mars. You are the propellant lead, '
    + 'so the plant is yours. In ten sols you write the handover the next crew works from. It '
    + 'says what the plant can really make, what slows it down, and what every batch was '
    + 'measured at before it went in the tank. One entry goes in a sol. Six people leave on '
    + 'what the tank holds.',
  ],

  ending: [
    'The vehicle was full eleven sols before the window. The last four hundred kilograms came from a '
    + 'catalyst charge nobody wanted to fit, running on water nobody had been cleaning properly, and '
    + 'both of those were found because somebody kept asking where the missing kilograms had gone.',
    'What it cost: thirty-eight sols of production lost to a poisoned bed, a spare charge used up '
    + 'with no second one on the planet, and a dust storm that took four sols of everything. What is '
    + 'unfinished: the water plant still has no alarm that fires before the salt comes through, only '
    + 'a rule written on paper, and the next crew will find out whether a rule is enough.',
    'And the reason there was a launch at all is that you did the arithmetic instead of joining the '
    + 'argument. You worked out how many sols the plant really needed, you found where the '
    + 'electricity was going, and you would not sign a claim about the new charge that was bigger '
    + 'than the evidence for it. Six people went home. That was your season.',
  ],

  look: {
    fov: 66,            // a 72° field distorts badly down a straight street
    near: 0.1,
    // Outdoors this has to reach past the horizon ranks and the sky dome. At an
    // interior's 160 the dome is clipped away entirely and the sky renders
    // black, in broad daylight, with no error anywhere. The dome here is 900.
    far: 1000,
    // Dust, at every distance. The colour is overridden every frame from
    // `atmosphere.haze`, so it matches the sky rather than sitting in front of
    // it; what this sets is how far the plain is legible for.
    fog: { colour: 0xc08154, near: 170, far: 700 },
    // Below 1.0 outdoors, or a mid albedo under a bright sky IBL blows out.
    // Lower than the Earth games: this ground is bright and the sky tint puts
    // warmth into every surface under it.
    exposure: 0.82,
    // How wide the player is, for collision. 0.45 suits a street; a place with
    // metre-wide doorways needs 0.3 or the player gets stuck in them.
    playerRadius: 0.45,
    // Six real lights is the ceiling. buildSunRig makes three of them.
    // Mars gets 43% of Earth's sunlight, and the dust scatters a great
    // deal of what arrives — so a weaker sun and a stronger hemisphere fill
    // than anywhere else in the set, which is also what fills the shadows the
    // way a dusty sky really does.
    lighting: { ambient: 0.10, sun: 2.4, hemi: 0.34, shadowExtent: 130 },
  },

  // Theme hooks. `decorate` is called by the outdoor world, the two fit-out
  // hooks by the interior one; the unused ones are ignored.
  decorate,
  fitOutRoom,
  fitOutSpine,
};
