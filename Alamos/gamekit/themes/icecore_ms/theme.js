// theme.js — Ice Core, at grade 6.
//
// edition-of: icecore
//
// GENERATED by tools/derive-edition.mjs. This is an *edition*: the place, the
// props, the interiors and the outfits are themes/icecore's and are imported
// across, so this directory owns a manifest and its generated content and
// nothing else. engine/dev/editionParity.mjs fails the game for any other file
// in here, and for a cast or a set of areas that has drifted from the base.
//
// The marker line above is read by engine/dev/registry.mjs (placeDir) and by
// vite.config.js, which take the site kind and the world module from the base
// theme — this directory's site.js is a re-export and no regex can read it.
import { site } from '../icecore/site.js';
import { OUTFITS, roleToOutfit } from '../icecore/outfits.js';
import { GROUPS } from './content/groups.js';
import { MISSIONS, WARMUPS } from './content/missions.js';
// tools/import-book.mjs writes all of these. BALLPARK_CALCS and JARGON must be
// imported or the estimates render un-answerable and no term is clickable.
import { CURRICULUM, BALLPARK_CALCS, JARGON } from './content/curriculum.js';
import { ROSTER, LEADERS, AVATARS } from './content/roster.js';
import { COPY } from './content/copy.js';
import { INTERIORS } from '../icecore/interiors.js';
import { decorate, fitOutRoom, fitOutSpine } from '../icecore/props.js';

export default {
  // Who this edition is for. `engine/core/typography.js` reads it and scales the
  // root font size, so the same game can ship at several reading levels.
  // grade 4 scales 1.18x, 7 scales 1.10x, 13 and up not at all.
  audience: { grade: 6 },

  id: 'icecore_ms',
  title: 'Ice Core',
  subtitle: 'Season Science Lead · Vestri Dome',

  // The place. `site.kind` picks the world module in vite.config.js:
  //   'outdoor'   engine/world/outdoorTown.js — buildings on terrain
  //   'interior'  engine/world/interiorSite.js — a spine with rooms off it
  // A theme whose place already exists may declare its own instead, with
  // `world: 'themes/<name>/world.js'` inside site.js. Deep Watch does.
  site,

  // Where the player starts the day, and which way they face. The day's budget
  // is measured from here, not from wherever the player is standing.
  start: site.spawn,

  content: { GROUPS, MISSIONS, CURRICULUM, BALLPARK_CALCS, JARGON, ROSTER, LEADERS, AVATARS, COPY, WARMUPS },

  people: {
    OUTFITS,
    roleToOutfit,
    // spawn must be >= ROSTER.length, or characters past the limit never appear
    // and any mission stop naming them is unreachable. Validated.
    spawn: ROSTER.length,
    // Background people. A narrow place needs far fewer: on the submarine more
    // than eight and the player cannot get down the passage.
    extras: 18,
  },

  // What is inside each room the player walks into, from book.yml. Rooms are
  // built by engine/world/interiorBuilding.js on first entry, in a district
  // four kilometres from the town.
  interiors: INTERIORS,
  // How those rooms are built: 'lab' (vinyl, screens), 'timber' (board walls,
  // chalkboards, no screens anywhere) or 'steel' (painted plate, deck matting).
  interiorStyle: 'steel',

  // The title card: ONE paragraph. What the player is, where they are, and what
  // happens if the work is not done — the situation, and nothing else. The
  // rules of a day (order, clock, the price of a wrong call) used to be a
  // second paragraph here and in every game, and it was the part nobody read:
  // four sentences of mechanics standing between the player and the game, all
  // of it discoverable in the first minute of play or from the plan card.
  opening: [
    'Vestri Dome is a camp on the polar ice. The crew drills the ice up in long cylinders and '
    + 'reads the weather of the past out of it. Two and a half kilometres of it has come up '
    + 'already, and none of it may ever get warmer than minus twenty. You are the season '
    + 'science lead, and the record this camp sends home is the one you sign. A camp four '
    + 'hundred kilometres away has published a record that does not agree with yours. Marit '
    + 'Halvorsen, the chief scientist, says a disagreement kept quiet is a decision made for '
    + 'everybody else. The plane comes in fifteen days. Anything not measured by then waits two '
    + 'years.',
  ],

  // The last thing anybody reads. Says what happened, what it cost and what is
  // unfinished, and takes all three from the fifteen days the player worked.
  ending: [
    'The season closed with the hole at 2,470 metres and a record that reaches, by counting, to '
    + 'about nine thousand nine hundred years, and by flow model to something under thirty '
    + 'thousand. The disagreement with Skarv turned out to be mostly the gas–ice age difference: '
    + 'four hundred and fifty-odd years at this site and a fraction of that at theirs, applied to '
    + 'both records and then checked against a tephra horizon that neither calculation produced. '
    + 'What was left of the difference was smaller than either group had claimed as its '
    + 'uncertainty, and the report said so in the hedged sentence rather than the confident one.',
    'What it cost: a night of the core store above minus twenty, which took the gas measurements '
    + 'from four sections with it; a fortnight in which the comparison was argued about on two '
    + 'timescales that could not be compared; and the deep dating, which still rests on a flow '
    + 'model this station did not build. What is unfinished: below the counted section every age '
    + 'is modelled, the firn air profile was never re-measured, and the next team arrives to a '
    + 'question nobody here could answer — whether the same correction holds at four times the '
    + 'accumulation rate, which is the assumption the whole comparison now stands on.',
    'And every line of it is yours. You found the correction that explained the disagreement. '
    + 'You checked both records against a marker neither calculation had produced. You wrote '
    + 'the careful sentence when the confident one would have got more attention. A season in '
    + 'the cold became a record other people can trust, because of you.',
  ],

  look: {
    fov: 66,            // a 72° field distorts badly down a straight street
    near: 0.1,
    // Outdoors this has to reach past the horizon ranks and the sky dome. At an
    // interior's 160 the dome is clipped away entirely and the sky renders
    // black, in broad daylight, with no error anywhere.
    far: 900,
    // Ice fog and blowing snow: a pale, close horizon with nothing behind it.
    fog: { colour: 0xd6dee6, near: 190, far: 620 },
    // Below 1.0 outdoors, or a mid albedo under a bright sky IBL blows out.
    // Snow is the brightest ground in the set and blows out first.
    exposure: 0.80,
    // How wide the player is, for collision. 0.45 suits a street; a place with
    // metre-wide doorways needs 0.3 or the player gets stuck in them.
    playerRadius: 0.45,
    // Six real lights is the ceiling. buildSunRig makes three of them.
    // A low sun and a bright surface: more bounce than anywhere else here.
    lighting: { ambient: 0.10, sun: 2.6, hemi: 0.42, shadowExtent: 140 },
  },

  // Theme hooks. `decorate` is called by the outdoor world, the two fit-out
  // hooks by the interior one; the unused ones are ignored.
  decorate,
  fitOutRoom,
  fitOutSpine,
};
