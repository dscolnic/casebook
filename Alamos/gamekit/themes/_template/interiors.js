// interiors.js — what is inside each room.
//
// PLACEHOLDER. `tools/import-book.mjs` overwrites this from the book's
// `interiors:` block, which is where it should be authored — the caption, the
// line on the case stand and the instrument reading belong beside the lesson
// they are about, not in a second file that drifts away from it.
//
// Keyed by group id. See tools/BOOK_TEMPLATE.md § "The optional blocks".
export const INTERIORS = {};
