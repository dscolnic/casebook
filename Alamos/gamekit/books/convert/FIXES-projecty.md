# Project Y — fix list

**Eight instruments went in and all eight work.** The four over-level passages
from the last list are fixed, and the CONTROL stop that was rejected now has
three candidates and applies cleanly. Nothing from the previous list is
outstanding.

One thing left.

## A question can be answered without the science

### `projecty.m13.s1` — "What Trinity had to answer" · `PROTOCOL`

The fourth pair of four is answered by its own wording. Three of the prompt's six
content words — **radiological**, **physical**, **site** — reappear in the option
that is keyed to it. A player who has read nothing can match on the repeated
words alone.

Reword one side of that pair so the match has to be made on the reasoning. The
usual fix is to change the *option*, not the prompt: say what the response
achieves rather than restating the situation it belongs to.

The other three pairs are fine.

---

## How to send it back

Work from your own `projecty-stops-edited.jsonl`, change only the rows named above, and return the
whole file the same way. Every row, changed or not.
