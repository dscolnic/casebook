// interactions.js — now the engine's, not this game's.
//
// byte-identical to the engine copy.
// Keeping a second copy here meant every fix had to be made twice; three times
// once the chemistry game existed. The forked original is beside this file as
// interactions.js.forked until the migration is proven.
export * from '../../../gamekit/engine/core/interactions.js';
