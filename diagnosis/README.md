# Diagnosis — clean schematic edition

Open `index.html`, or send any individual `*_playable.html` as a standalone game.

The graphics were rebuilt to prevent text, arrows, and numbered reading badges from covering one another. Editable `dpack_*.js` files are included.
